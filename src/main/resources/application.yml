spring:
  # 'application-oauth2.yml의 설정값을 사용하기 위한 값 추가'
  profiles:
    include: oauth2

  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password: